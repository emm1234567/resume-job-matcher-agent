# 简历解析与岗配评估 Agent 依赖
pydantic>=2.5,<3
openai>=1.12            # 兼容 OpenAI 接口的任意模型服务（OpenAI/DeepSeek/Moonshot/vLLM 等）
pdfplumber>=0.10        # PDF 文本抽取
python-docx>=1.1        # Word(.docx) 文本抽取
rank-bm25>=0.2.2        # 关键词召回（Embedding 不可用时的降级方案）
numpy>=1.24             # 向量相似度计算
python-dotenv>=1.0      # 加载 .env 配置
jieba>=0.42             # 中文分词（提升 BM25 召回质量；缺失时自动降级为正则切分）
fastapi>=0.100.0         # Web 框架
uvicorn>=0.23.0          # ASGI 服务器
python-multipart>=0.0.5 # FastAPI 文件上传依赖
